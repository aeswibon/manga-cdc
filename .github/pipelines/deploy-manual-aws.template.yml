version: 2
companion_workflows:
  - .github/workflows/deploy-manual.yml
pipelines:
  deploy-manual-aws:
    stages:
      - id: deploy-aws
        workflow: .github/workflows/stage-deploy-aws.yml
        inputs:
          image_tag: ${IMAGE_TAG}
          deployment_target: ${DEPLOYMENT_TARGET}
          deploy_method: ${DEPLOY_METHOD}

      - id: deploy-vercel
        workflow: .github/workflows/stage-deploy-vercel.yml
        needs:
          - deploy-aws
