name: Watchlist validation

on:
  pull_request:
    paths:
      - "data/watchlist.yaml"
      - "scripts/validate-watchlist.py"
      - "scripts/test_validate_watchlist.py"
      - ".github/workflows/watchlist-check.yml"
  push:
    branches: [master]
    paths:
      - "data/watchlist.yaml"
      - "scripts/validate-watchlist.py"
      - "scripts/test_validate_watchlist.py"
      - ".github/workflows/watchlist-check.yml"

jobs:
  validate-watchlist:
    name: Lint watchlist.yaml
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Lint data/watchlist.yaml
        run: python scripts/validate-watchlist.py

      - name: Watchlist linter unit tests
        run: python -m unittest scripts/test_validate_watchlist.py
