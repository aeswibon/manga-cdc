# Terraform (preview)

Cloud-specific Terraform modules for GCP, AWS, and Azure are planned for a future release.

The configure wizard no longer generates `terraform.tfvars` in v1. Use Docker Compose or Helm with your own managed PostgreSQL and eventing services.

The existing DigitalOcean Kubernetes scaffold in this directory is a placeholder and not maintained as part of the flexible deployment v1 workflow.
